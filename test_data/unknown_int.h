#include <sys/types.h>
#include <stddef.h>

typedef ptrdiff_t pointy;

struct structy {
  int a;
  heart_t heart;
  long b;
};

brown_t* funcy(int x, pants_t* brown, unsigned long abc);

typedef char (*f_ptr)(pants_t b, char c);

typedef ssize_t super_size;